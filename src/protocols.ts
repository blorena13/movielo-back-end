export type AplicationError = {
    name: string;
    message: string;
};

export type UserType = {
    email: string;
    profileImage: string;
    name: string;
    username: string;
}

export type SessionType = {
    email: string;
    password: string;
}